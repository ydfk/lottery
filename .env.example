# Docker Compose 挂载目录与端口示例。
# 这份文件只用于容器发布，不参与本地前后端开发。

# 应用对外暴露端口
LOTTERY_APP_PORT=25610

# 宿主机配置目录，会挂载到容器内 /app/config
LOTTERY_CONFIG_DIR=./backend/config

# 宿主机数据目录，会挂载到容器内 /app/data
LOTTERY_DATA_DIR=./backend/data

# 宿主机日志目录，会挂载到容器内 /app/log
LOTTERY_LOG_DIR=./backend/log
